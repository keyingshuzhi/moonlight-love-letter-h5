# Background Music

将需要使用的背景音乐统一重命名为：

```text
music.wav
```

然后放入当前 `music/` 目录：

```text
music/
├── music.wav
└── README.md
```

页面会自动读取 `music/music.wav`，无需修改 HTML、CSS 或 JavaScript。

> 请确保使用的音乐拥有合法使用授权。公开 GitHub 仓库建议不要提交无再分发授权的音乐文件。
